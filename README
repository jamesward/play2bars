Run on Heroku
-------------

1) Create a new app:

        $ heroku create

2) Push the app:

        $ git push heroku java-ebean-mysql:master

3) Add the ClearDB Add-on:

        $ heroku addons:add cleardb:ignite

